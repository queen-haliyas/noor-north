{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "git push origin main",
            "statusMessage": "Pushing to origin main..."
          }
        ]
      }
    ]
  }
}
